# Sync Log — 2026-04-28（Win機側）

> 目的: MBA 側ブランチとマージするときの衝突予防＆進捗共有。
> 作業環境: Win PC（ビルド・dev起動なし、コミットまで）
> ベースコミット: `767bb24`（v0.20.0 大規模 UX/a11y 改善バッチ） より以降

---

## 🍎 明日のMBA作業タスクリスト

> Win 側 push 済の最新コミット: 取り込み後に作業開始

### Step 1: コード取り込み

```bash
cd ~/dev/20260401-Project-Blueprint
git pull --rebase origin main
# Win 側の今日 50+ コミット分を取り込み
git log --oneline -10  # 最新コミットが Win の最終 push と一致することを確認
```

### Step 2: 環境セットアップ確認

```bash
cd otetsudai-quest-mobile
npm install              # 念のため依存最新化（Win機で package.json は変更なし）
```

### Step 3: 実機検証（必須項目）

[testflight-v3-checklist.md](../../otetsudai-quest-mobile/docs/testflight-v3-checklist.md) を全項目チェック。
特に重点確認:

- [ ] **取引/金庫/錬成** 4ボタンが冒険と並んで揃っている
- [ ] 戻るボタン4階層（🏠/🚪/✕/←）が画面ごとに正しく動く
- [ ] **ペット P4タップ喜び**：ハート3個飛ぶ
- [ ] **ペット P5進化フラッシュ**：ステージ移行時のゴールド光
- [ ] **親承認 S4✓バースト**：承認時のチェックマーク演出
- [ ] **WalletTransferModal**: 3カード選択（from/to/amount）で即実行、移す/やめるボタン無し、✕右上
- [ ] **家族メッセージ送信**: スタンプ/メッセージ/両方の3パターン
- [ ] **親承認FK違反バグ**: code 23503 が再発しない

### Step 4: DB マイグレーション適用

```bash
cd otetsudai-bank
supabase db push  # または Supabase ダッシュボードから実行
# 適用対象: 20260419_family_messages_nullable.sql
```

### Step 5: TestFlight v3 ビルド & 配信

```bash
cd ../otetsudai-quest-mobile
eas build --platform ios --profile preview
# autoIncrement: true なので build 番号 9→10 へ自動 +1
# ビルド時間: 15〜20分

# 完了後
eas submit --platform ios --profile production --latest
# App Store Connect の TestFlight に送信
```

### Step 6: TestFlight ユーザー向けリリースノート投稿

testflight-v3-checklist.md 内の「リリースノート」セクションを参照。
v0.26.0 のハイライト6点（ブランド／戻るボタン／ウォレット簡素化／ペット演出／家族メッセージ／視認性）を投稿。

### Step 7: 明日中にやりたい新機能（時間あれば）

#### 7-A: WalletTransferModal 動作確認後の追加調整
- 即時実行になったので、誤操作防止用に「ぜんぶ」だけは確認ダイアログ追加検討
- プリセット選択時のフィードバック（軽い hapticImpact）追加検討

#### 7-B: ペット図鑑（Win 機で着手中、引き継ぎ可能）
Win 機で `src/lib/pet-encyclopedia.ts` 新規作成済（既存 `otetsudai_pets` テーブルから図鑑状態を導出）。
未完了タスク:
- `src/components/PetEncyclopediaModal.tsx` 新規実装（6種×4段階のグリッド表示）
- PetManagementModal に図鑑ボタン追加
- silhouette / 解放表示の3状態 UI

#### 7-C: note 記事の校正と投稿
[note-20260428.md は削除済] 画面エクスポート版を Win 側で保存している前提。
❶（編集長）❼（リズム整え）も適用してから note.com 投稿。

### 衝突注意ファイル（高リスク）

| ファイル | Win 側変更内容 | MBA 側で同箇所触っていたら衝突 |
|---|---|---|
| `ChildDashboardScreen.tsx` | Quick Nav リブランド + IconBox + 振替動線 | 中〜高 |
| `ParentDashboardScreen.tsx` | FK違反対策 + 楽観的UI + ✓バースト | 中〜高 |
| `WalletDetailScreen.tsx` | お財布→金庫、3pot rebrand、IconBox | 中 |
| `LoginScreen.tsx` | 戻るボタン補強 4箇所 | 中 |
| `app.json` | version 0.14.0 → 0.26.0 | 低（数字差し替えのみ） |

---

## 1. 今日のコミット済み変更（push 済 + 午後追加分）

> Win → origin: 同期済。午後の作業は変更ごとに本ファイルに追記する運用。

### 午後追加（2026-04-28 14:00以降）

#### コミット LL: `b3980b1` — feat(pet): P6 ペット食事アニメ追加

- PetDisplay.tsx に prevFedCount useRef + useEffect で fed_count 増加検出
- 増加時に eatBob で translateY 2連 bob（-8→0→-6→0、600ms）
- 🍖 emoji を右上 absolute 配置で食事中表示
- useReducedMotion 尊重、リセット時(降順)は演出スキップ
**衝突可能性**: 低（PetDisplay 単独）

#### コミット KK: `a3e3faa` — feat(transfer-modal): Haptic feedback 追加

- WalletTransferModal の3カード選択動線に触覚フィードバック追加
- pot 選択: selectionAsync / amount 選択: ImpactMedium
- 転送成功: NotificationSuccess / 失敗: NotificationError
- .catch(() => {}) で権限不可端末でクラッシュ防止
**衝突可能性**: 低

#### コミット JJ: `5d9b6fe` — fix(pet-encyclopedia): SVG収まり改善＋卵を未発見扱い

- フォントサイズ縮小（cellName 12→9、cellStage 10→8、cellCount 9→7）
- cell aspectRatio 0.85→0.7 で縦長化、padding 8→6 で余白圧縮
- pet-encyclopedia.ts: VISIBLE_STAGES = baby+ のみ集計、卵は未発見扱い
**衝突可能性**: 低

#### コミット II'1: `352823a` — feat(pet): ChildDashboard 図鑑ボタン＋PetManagement 漢字ルビ化

#### コミット II: `0c06e46` — feat(pet-encyclopedia): ペット図鑑モーダル新規実装

- PetEncyclopediaModal.tsx 新規（6種×3列グリッド、未発見はシルエット）
- pet-encyclopedia.ts の lib で otetsudai_pets から状態導出（新DBなし）
- 進捗バッジ「発見 N/6」「完全成長 N/6」上部表示
- 完全成長(adult)はゴールド枠強調
- 全テキスト漢字+ルビ（図鑑/発見/完全/成長/卵/赤ちゃん/子ども/大人/匹/仲間/頑張）
- PetManagementModal に図鑑ボタン追加（accent枠+star）
- 既存「ペットずかん」ヘッダー→「仲間ペット」、「1ぴき」→「1匹（ひき）」
**衝突可能性**: 低（新規ファイル＋既存箇所軽微変更）

#### コミット HH: `229f629` — docs(sync): MBA タスクリスト＋pet-encyclopedia.ts logic

#### コミット GG: `026f45c` — fix(transfer-modal): 移すボタン削除、OK/プリセット即時実行

#### コミット FF: `baf3240` + `980253a` — fix(transfer-modal): ✕右上配置＋やめる削除

#### コミット EE: `e8d2504` — feat(pet): P5 ペット成長段階移行アニメ

- PetDisplay.tsx に prevStage useRef + useEffect で変化検出
- 進化時: scale 0.6→1.5→1.0 spring + ゴールドフラッシュ opacity 1→0
- evolveScale を既存 scale と Animated.multiply で合成
- useReducedMotion 尊重、初回レンダーは演出スキップ
**衝突可能性**: 低（PetDisplay 単独）

#### コミット DD: `5aa6999` — feat(parent-anim): S4 親承認成功時の✓バースト演出

- ApprovalSuccessBurst.tsx 新規（120x120 ✓リング、星3個放射、バナー）
- ParentDashboardScreen handleApprove 末尾で showApprovalBurst をtrigger
- spring scale で 0→1.3→1.0、星 60px 飛散＋360deg 回転、バナー 1.5s 表示
- useReducedMotion 対応
**衝突可能性**: 中（ParentDashboard、MBA でも触られる可能性）

#### コミット CC: `2d9a2b5` — feat(web-brand): parent page 3色残高 RPG強テーマ並列表記

- otetsudai-bank/app/parent/page.tsx 3色残高ラベル
- 使う→取引（使う）、貯める→金庫（貯める）、増やす→錬成（増やす）
- 親はビジネス的言葉が直感的なので並列表記で旧名キープ
**衝突可能性**: 低（ピンポイント）

#### コミット BB: `412b123` — docs(release): TestFlight v3 配信チェックリスト v0.26.0

- otetsudai-quest-mobile/docs/testflight-v3-checklist.md 新規
- MBA で実行する配信フローを文書化（pull/build/submit 各手順）
- 実機検証チェックリスト（v0.26.0 変更点 + リグレッション）
- DB migration 適用確認、リリースノート案、配信後アクション
**衝突可能性**: 低（新規ファイル）

#### コミット AA: `7f1dc6b` — feat(web-brand): daily-login-modal/shop-dialog 並列表記

- daily-login-modal.tsx: 「つかう」→「取引（つかう）」
- shop-dialog.tsx: 「つかう」のおかね→「取引（つかう）」のおかね
- money-tree（ふやすの木）はメタファー保護のため据え置き
**衝突可能性**: 低

#### コミット Z: `2e5a227` — feat(web-brand): RewardSplitSlider に RPG強テーマ並列表記

- otetsudai-bank/components/reward-split-slider.tsx
- 「貯める: N%」→「金庫（ためる）: N%」、「増やす: N%」→「錬成（ふやす）: N%」
- ヒント文「使うお金」→「取引・使うお金」
- aria-label 更新（金庫/錬成 + 補足）
**衝突可能性**: 低

#### コミット Y: `bf172d4` — feat(web-brand): Web版 child page に RPG強テーマ反映

- otetsudai-bank/app/child/[childId]/page.tsx
- 3pot ラベル: 使う/貯める/増やす → 取引/金庫/錬成（複数 R で漢字＋ルビ表示）
- 補強サブテキスト追加: しょうにんと とりひき/たからを しまう/お金を そだてる
- 「使う」CTA ボタン中身は動詞のまま維持
- ロジック無変更、文言＋補強のみ
**衝突可能性**: 中（child page は MBA でも触られる可能性、ただし変更箇所はピンポイント）

#### コミット X: `cd6e1fc` — feat(pet): P4 ペットタップ時の喜び演出

- PetDisplay.tsx: 孵化済ペット tap 時に scale bounce + ハート3個放射演出
- HappyEffect ローカルコンポーネント追加（Animated.Value × 3 hearts）
- 200ms 後に既存 onManage（PetManagementModal）発火、UX体感UP
- useReducedMotion 対応（true 時はスキップ）
**衝突可能性**: 低（PetDisplay 単独、MBA 側で同箇所未編集）

#### コミット W: `46a7756` — fix(parent-approve): FK違反(approved_by_fkey) 完全対策

- ParentDashboardScreen に resolveDbUserId() ヘルパ追加
- session.userId が auth.users.id を保持する不整合を auth_id 経由で解決
- handleApprove / handleApproveSpend で正しい otetsudai_users.id を使用
- handleApproveSpend にエラーアラート＋楽観的UI（pendingSpends即削除）
**衝突可能性**: 中（ParentDashboard、MBA でも触られる可能性高）

#### コミット V: `a5568ae` — docs(release): v0.26.0 README＋app.json bump

- mobile README に v0.26.0 エントリ追加（v0.25.0 の下に1行）
- app.json version: 0.14.0 → 0.26.0（メタ情報のみ、name/slug 据え置き）
**衝突可能性**: 低

---

### 朝のコミット履歴（push 済）

> Win → origin: 22コミット先行 / 0遅れ。MBA 側へは push 後に pull/rebase で取り込み。

### コミット U: `e7fef66` — fix(nav): 戻るボタンの動作・アイコン・補強を全画面で統一

統一ルール（HCD準拠）:
- 🏠 PixelHouseIcon: ChildDashboard 直行 + 「(TOPへ)」補強
- 🚪 PixelDoorIcon : goBack() + 「(まえへ)」補強
- ✕ PixelCrossIcon: モーダル close + 「とじる」
- ← Unicode arrow: onboarding/wizard step戻り（補強不要）

修正:
- InvestScreen Order Modal の🏠誤用を ✕「とじる」に
- InvestScreen Lock画面に🚪+補強追加
- LoginScreen 4箇所（family x2, member, pin）に「(まえへ)」追加
- LoginScreen styles に backHint 追加

**衝突可能性**: 中（LoginScreen / InvestScreen ヘッダー JSX、MBA側で同箇所変更があると衝突）


> 今日のWin作業は全て「UI/UX 微調整」レイヤー。DB スキーマ・認証には未着手。
> 末尾でナビ構造を1箇所だけ調整（戻るボタンを ChildDashboard 直行）。

### コミット T: `0998a57` — fix(nav): 🏠戻るボタンに「(TOPへ)」補強テキスト追加

家アイコンだけでは「どこに戻るか」が暗黙的だった問題を、ラベル下に補強で明示。
- レイアウト: 🏠 + [もどる / (TOPへ)] (9pt opacity 0.7)
- もどる 16→14pt、3画面で統一
**衝突可能性**: 中（複数画面のヘッダーJSX）

### コミット S: `a1b5aab` — fix(nav): HCD準拠で🏠戻るボタンを ChildDashboard に直行

🏠アイコン＝「家に帰る」の認知モデルと goBack() の不一致を解消。
`navigation.navigate("ChildDashboard", { childId })` で1タップで子ダッシュへ。
**衝突可能性**: 中（goBack→navigate に変更、MBA側でナビ構造変更があると要調整）



### コミット R: `4bbfe0f` — fix(invest/spend/wallet): 遷移先ヘッダーアイコンを固定枠でラップ＋TS エラー解消

- WalletDetail: PixelCoinIcon を 22×22 枠で
- InvestScreen: PixelSeedlingIcon を 18×18 枠
- SpendRequest: PixelCoinIcon を 18×18 枠
- InvestScreen の useFocusEffect 参照順序エラー（TS 2448）を loadData 宣言直後に移動して解消
**衝突可能性**: 中（複数画面のヘッダー JSX）

---

### コミット Q: `1f366f4` — fix(wallet-detail): 3pot SVG アイコンも 20×20 固定枠で揃える

WalletDetail 3pot カード（取引/金庫/錬成）のアイコン位置不揃い問題を pocketIconBox で解消。
**衝突可能性**: 低（追加スタイル + JSX ラップのみ）

---

### コミット P: `6fc941a` — fix(quick-nav): 4SVGアイコンの寸法差を 24×24 固定枠で吸収しテキスト位置揃え

ラベル位置不揃いの根本原因が SVG grid 寸法差（4×6/5×5/6×6/5×6）と判明。
quickNavIconBox (24×24) でラップしテキスト位置を完全一致。
**衝突可能性**: 中

---

### コミット O: `01af5e7` — feat(brand): 遷移先タイトル4箇所を 冒険/取引/金庫/錬成 に統一

- ChildDashboard quest section: 「X のクエスト」→「X の 冒険」
- SpendRequest: 「使いたい！」→「取引」
- WalletDetail header: 「お財布」→「金庫」
- InvestScreen header: 「投資」→「錬成」

合わせて refreshBtnLabel 13→10pt / refreshBtnHint 9→6pt に縮小。
**衝突可能性**: 中（複数画面のヘッダー文言）

---

### コミット N: `d304b1e` — fix(child-dash): 点検／移すボタンのヒント文字数を揃え枠内に収める

「移す」のヒントを「（おかねを 移動する）」に短縮。両ボタンほぼ同字数に。
**衝突可能性**: 低

---

### コミット M: `149da5f` — fix(brand): 4ラベル全部 split ruby に統一しベースラインずれを解消

全ラベルを `[["X","yy"],["Z","ww"]]` 形式の split ruby に統一（後に SVG 原因と判明し IconBox で根本解決）。
**衝突可能性**: 低

---

### コミット L: `94d77ba` — feat(brand): 3pot を Option5（名詞統一）の RPG 強テーマに刷新

- 使う → 取引（とりひき）
- 貯める → 金庫（きんこ）
- 増やす → 錬成（れんせい）
- 冒険（ぼうけん）は維持

補強サブテキスト（RPG 強テーマ版）:
- 冒険: クエストに ちょうせん
- 取引: しょうにんと とりひき
- 金庫: たからを しまう
- 錬成: お金を そだてる

quickNavHint スタイル新設、minHeight 100、justifyContent flex-start で4ボタン上揃え。
**衝突可能性**: 高（3pot の用語ブランド変更、MBA 側で同期しないと表現が混在）

---

### コミット K: `a040e82` — fix(child-dash): 点検／移すボタンの括弧つきヒント 10→9pt

`refreshBtnHint` のフォントサイズを 10→9pt に縮小、lineHeight 12 + marginTop 2 で
ラベル（13pt）との階層を明確化。
**衝突可能性**: 低（スタイル定義のみ、MBA 側で同箇所を触っていなければ無害）

---

### コミット J: `3913e20` — feat(quick-nav): 「稼ぐ」を「冒険／ぼうけん」に変更＋下段1行固定

- ラベル: 「稼ぐ」→「冒険」（漢字＋ルビ、世界観の統一）
- サブテキスト: 「クエスト」→「クエストへ」（行き先を明示）
- 4ボタン下段（クエストへ／円表示）に numberOfLines + adjustsFontSizeToFit
- accessibilityLabel も同期
**衝突可能性**: 中（ChildDashboard 中央集権ファイル）

---

### コミット I: `6b62167` — fix(quick-nav): 4ボタン下段のテキストラインを統一

- quickNavSub と quickNavAmount を fontSize 11 / lineHeight 14 / marginTop 2 で統一
- 数字に「円」suffix を追加し文字種を視覚的に揃える
**衝突可能性**: 中（同上）

---

### コミット H: `a39d72c` — feat(transfer): 「振替／ふりかえ」を「移す／うつす」に統一

ChildDashboard と WalletTransferModal の銀行用語感を排除し、世界観中立の日常語へ。
- ボタン: 「振替」→「移す」
- モーダルタイトル / 確定ボタン / エラー文言 / accessibilityLabel すべて連動
**衝突可能性**: 低（モーダルは新規ファイル、ボタン文言はピンポイント）

---

### コミット G: `514ea13` — feat(child-dash): 「更新」を「点検」に変更

子ども画面の銀行用語感を排除し RPG 世界観の装備チェック感「点検／てんけん」へ。
- ラベル / サブテキスト / accessibilityLabel / Hint 全部同期
- 振替は別途「移し替え」案を検討中の旨をコミットメッセージに明記（→ 後に「移す」採用）
**衝突可能性**: 中

---

### コミット F1: `accb992` — feat(wallet-detail): 3pot カードに「何のため」説明文を追加

10pt textMuted で「使う＝ほしいものを かう」「貯める＝あとで つかう」「増やす＝かぶで そだてる」。
**衝突可能性**: 低（pocketHint 新規スタイルのみ追加）

---

### コミット F2: `4f5eca2` — refactor(wallet-detail): 「全部のお金」カード削除＋ヘッダー総額統合

380（総額）と376（ためる）が4円差で並ぶ視覚混乱を解消。
- 独立 totalCard を削除、総額をヘッダー右に rf(22) ゴールドで統合
- レイアウト：「[もどる] [💰お財布 380円] [spacer]」
**衝突可能性**: 中（同ファイル MBA 側でも編集中の可能性）

---

### コミット E: `ee31f4c` — fix(wallet-transfer): ソフトウェアキーボード干渉を解消

**変更ファイル（1件、+55 / -7）**

| ファイル | 内容 |
|---|---|
| `otetsudai-quest-mobile/src/components/WalletTransferModal.tsx` | KeyboardAvoidingView＋ScrollView＋背景タップ閉じ＋onFocus自動スクロール＋OKボタンでキーボード閉じ |

**症状**: 振替モーダルで金額入力時、キーボードがテキストボックスと決定ボタンを覆い操作不能。
**対応**:
- KeyboardAvoidingView (iOS=padding / Android=height) で高さ自動確保
- ScrollView 内部スクロール（bounces=true、keyboardShouldPersistTaps=handled）
- 背景タップで Keyboard.dismiss
- TextInput onFocus で scrollToEnd
- 金額行に「OK」ボタンで明示的閉じる
- returnKeyType="done" + onSubmitEditing でハードキーでも閉じる

**衝突可能性**: 低（新規コンポーネント単独修正、MBA側にこのファイルが無いため）

---

### コミット D: `d8efc79` — feat(mobile): MBA同期用バッチ — 楽観的UI/テーマ切替/3pot振替/承認エラー処理

**変更ファイル（7件、+465 / -14）**

| ファイル | 種別 | 主な内容 |
|---|---|---|
| `otetsudai-quest-mobile/src/components/WalletTransferModal.tsx` | 新規 | 3pot間振替UI（バリデーション込み） |
| `otetsudai-quest-mobile/src/components/RpgButton.tsx` | 修正 | `contentAlign` prop 追加（非破壊） |
| `otetsudai-quest-mobile/src/screens/ChildDashboardScreen.tsx` | 修正 | 楽観的UI（submittedTodayIds）、テーマ切替UI、handleTransfer |
| `otetsudai-quest-mobile/src/screens/ParentDashboardScreen.tsx` | 修正 | 承認／却下のエラー表示＋pendingLogs 即時除去 |
| `otetsudai-quest-mobile/src/screens/InvestScreen.tsx` | 修正 | useFocusEffect で復帰時に loadData |
| `otetsudai-quest-mobile/src/screens/LandingScreen.tsx` | 修正 | RpgButton の contentAlign="start" 適用 |
| `otetsudai-quest-mobile/src/theme/palettes.ts` | 修正 | dungeon 単独 → 4種公開 |

**MBA 側でやるべきこと**
1. `git pull --rebase origin main`
2. Expo 起動して以下を確認
   - クエスト完了報告で押した分が一覧から即消える
   - 親承認で承認キューから即消える、エラー時にalert表示
   - 投資画面に遷移し直すと残高が最新化
   - 子ヘッダーのテーマボタン4つで切替可能
   - 振替モーダルで3pot間の振替成功

**衝突可能性**
- `ChildDashboardScreen.tsx` / `ParentDashboardScreen.tsx` は中央集権ファイルで MBA 側でも編集中の可能性高（**中〜高**）
- `palettes.ts` の forest/adventure/breeze 定義が MBA 側にも存在するか要確認
- `RpgButton.tsx` は非破壊変更だが共有なので軽微衝突あり得る

---

### コミット A: `825f80f` — fix(family-stamp): メッセージ送信を3パターン全て成功させる

**変更ファイル（3件）**

| ファイル | 種別 | 主な内容 |
|---|---|---|
| `otetsudai-bank/supabase/migrations/20260419_family_messages_nullable.sql` | 新規 | stamp_id / message を nullable 化 + CHECK 制約「どちらか必須」 |
| `otetsudai-quest-mobile/src/components/FamilyStampSendModal.tsx` | 修正 | canSend 堅牢化（!! + length>0）、エラーハンドリング追加、エラー表示UI |
| `otetsudai-bank/components/family-stamp-relay.tsx` | 修正 | 同上のWeb版対応 |

**MBA側でやるべきこと**
1. `git pull`（または `git rebase`）
2. SQLマイグレーション適用：`supabase db push` または Supabase ダッシュボードで実行
3. 実機でスタンプのみ／メッセージのみ／両方の3パターン送信を検証

**衝突可能性**
- `FamilyStampSendModal.tsx` と `family-stamp-relay.tsx` を MBA 側で同時編集していなければ衝突なし
- マイグレーションは新規ファイルなので衝突なし

---

## 2. 今日のローカル未コミット変更（作業中）

> 用途不明な変更が混在。MBA 側にどれが必要か要判断。

### ファイル A: `otetsudai-quest-mobile/src/theme/palettes.ts`

**差分（1行）**
```diff
- export const palettes = { dungeon } as const;
+ export const palettes = { dungeon, forest, adventure, breeze } as const;
```

**意図**: テーマ4種すべてを公開（テーマ切替UI復活の準備）。
**MBA 側で同等変更が既にあれば**：そちらを優先。
**MBA 側で `dungeon` 単独が現役なら**：このローカル変更は捨てて MBA に合わせる。

---

### ファイル B: `otetsudai-quest-mobile/src/screens/ChildDashboardScreen.tsx`

**追加 25行（+25 / -1）の概要**
- `useTheme()` から `paletteName` と `setPalette` を取得
- ヘッダーの `rightSlot` にテーマ切替の小さな丸4つ（dungeon / forest / adventure / breeze）を配置
- 選択中はボーダー2px強調

**意図**: 子画面ヘッダーから瞬時にテーマ切替可能にする UX 強化。
**MBA 側で同等のテーマ切替ロジックがあれば**：そちらを優先しこちらは捨てる。
**MBA 側にない場合**：採用する／しないをUX判断で決める。

---

### ファイル C: `otetsudai-quest-mobile/src/components/RpgButton.tsx`

**差分**: `contentAlign?: "center" | "start"` prop 追加。`start` 時は `alignSelf: stretch + justifyContent: flex-start + gap: 12` で内部コンテンツを左寄せ。デフォルト center で既存呼び出しに影響なし。
**意図**: LandingScreen の はじめての人 / つづきから の **アイコン位置と行頭を完全揃え** するため。
**衝突可能性**: 中（共有コンポーネントだが追加 prop なので非破壊）

---

### ファイル D: `otetsudai-quest-mobile/src/screens/LandingScreen.tsx`

**差分**: 2つの RpgButton に `contentAlign="start"` を指定し、Text に `textAlign: "left"` を追加。
**意図**: 上記 C と連動。文字数違い（はじめてのひと=7字 / つづきから=5字）でもアイコン左端で揃う。
**衝突可能性**: 低

---

### ファイル B（追記）: `ChildDashboardScreen.tsx` — 当日後半の追加変更

上の B（テーマ切替）に加え、以下を追加：

**B-2. クエスト完了時の即時非表示（楽観的UI）**
- state `submittedTodayIds: string[]` 追加
- `loadData()` で「今日 created_at 以降の status in (pending, approved)」の task_id を取得し state へ **マージ**（DB読み取り遅延でも消えないように `Set` で union）
- `handleComplete()` 冒頭で optimistic に `task.id` を追加
- レンダリング側 4 箇所（special / 通常の filter+map と visibility 判定）で `!submittedTodayIds.includes(t.id)` を AND
- **理由**: クリア後も同じクエストが残って何度も押せてしまう問題の解消
- **既知の制約**: 親承認前なのでウォレットは増えない（次の TODO 参照）
**衝突可能性**: 中（同ファイルを MBA 側で大改修中の可能性高）

---

## 3. 直前のベース（767bb24 以降に Win 側で起きたこと）

```
825f80f  11:22  fix(family-stamp): メッセージ送信を3パターン全て成功させる   ← コミット済み
（ローカル未コミット 4ファイル）                                          ← 上記2.
```

**MBA 側との分岐点**: `767bb24` (v0.20.0 README更新) 直後。MBA 側は別の系統で
オンボーディング・Job Saga・Edge Function SHA-256・admin強化などが進んでいる
（=`bb045b9` `4711493` `8171aea` `1e155ed` 等）。

---

## 4. マージ戦略の提案

### 推奨: MBA 主導でリベース

1. Win 側のコミット `825f80f` を保持したまま `git fetch origin`
2. MBA 側の最新を取得して、**MBA を上流**として `git rebase` で Win コミットを乗せる
3. 未コミット変更（ファイルA〜D）は、MBA 側で同等変更が無いか確認してから適用
4. SQL マイグレーション `20260419_family_messages_nullable.sql` は MBA 側に既存の同名ファイルが無ければそのまま push
5. 実機検証は MBA 環境で行う

### 衝突しやすい候補

| ファイル | 理由 |
|---|---|
| `palettes.ts` | テーマ追加の変更を両機でやっている可能性 |
| `ChildDashboardScreen.tsx` | 大量変更が両機で起きやすい中央集権ファイル |
| `package-lock.json` / `package.json` | 依存追加が両機で起きていれば衝突確実 |

### 衝突が起きにくい候補

| ファイル | 理由 |
|---|---|
| `FamilyStampSendModal.tsx` | 単機能コンポーネントで分離している |
| `family-stamp-relay.tsx` | 同上 |
| 新規SQLマイグレーション | 新規ファイル |

---

## 5. 実機検証チェックリスト（MBA で）

- [ ] `supabase db push` で `20260419_family_messages_nullable.sql` を適用
- [ ] スタンプのみ送信 → 成功する
- [ ] メッセージのみ送信 → 成功する
- [ ] スタンプ＋メッセージ送信 → 成功する
- [ ] 受信者未選択時に送信ボタンが disabled
- [ ] insert 失敗時に赤字エラーが表示される（DB一時切断などで再現）

---

## 6. 残タスク（明日以降）

- [ ] スプシ #54 / #55 の TRUE 化（実機検証後）
- [ ] テーマ切替UI（未コミット分）の方針決定
- [ ] Win 側 `git push origin main`（MBA とリベース後）
- [x] **完了**: 親側 `handleApprove` / `handleReject` に楽観的UI（pendingLogs から即削除）＋ DB 更新失敗時のエラーアラート追加（ParentDashboardScreen.tsx）
- [x] **完了**: ふやす画面のウォレットが親承認後に更新されない問題を修正 — `useFocusEffect` で画面復帰時に `loadData()` を再呼び出し（InvestScreen.tsx）。同様のパターン（route param スナップショット）が SpendRequestScreen / WalletDetailScreen にもあり、ユーザー報告があれば同じ対応をする
- [x] **完了**: 子ダッシュボードに「おさいふを こうしん」ボタン追加（ChildDashboardScreen.tsx）
- [x] **完了**: 親承認 FK違反バグ修正（`code 23503` `approved_by_fkey`）
  - **根本原因**: `LoginScreen.handleAdminLogin` が `session.userId` に **`auth.users.id`** を保存していたが、`otetsudai_task_logs.approved_by` は **`otetsudai_users.id`** を要求。recent commit `495b937` で「RLS不要」と称して `otetsudai_users` の lookup を省略したのが原因
  - **修正1（即解決）**: `ParentDashboardScreen` に `resolveDbUserId()` ヘルパ追加 — `session.authId` で `otetsudai_users` を引いて正しい id を取得。`handleApprove` / `handleApproveSpend` で使用。再ログイン不要で既存セッションでも動く
  - **修正2（根本解決）**: `LoginScreen.handleAdminLogin` で親ログイン時に `otetsudai_users` を `auth_id` で引いて `dbUserId` / `familyId` / `displayName` を取得しセッションに保存。次回ログインから正しい id が入る
  - `handleApproveSpend` にもエラーハンドリング＋pendingSpends 楽観的削除を追加
  - **衝突可能性**: 高（LoginScreen と ParentDashboard 両方変更、MBA 側の認証周りと衝突しやすい）
- [x] **完了**: 銀行/証券モデルの **ポット間ふりかえ** 機能追加
  - 新規: `otetsudai-quest-mobile/src/components/WalletTransferModal.tsx`（つかう⇄ためる⇄ふやす を任意に移動）
  - ChildDashboard: ふりかえボタンを こうしん の隣に配置、`handleTransfer(from, to, amount)` 実装、wallet 残高更新＋transactions ログ
  - こうしんボタンの hint を「（おうちのひとの承認をとりこむ）」→「（さいきんの きんがくに する）」に中立化
  - **DB 変更なし**（既存の3pot間で完結）。「うけとり」用第4potの追加は MBA 合流後に検討
  - **衝突可能性**: 中（ChildDashboard 大改修中の MBA と衝突の可能性、新規ファイルは衝突なし）
  - 配置: Quick Nav 直下、ScrollView 外で常時可視
  - 動作: `onRefresh` 共有（pull-to-refresh と同じ `loadData`）→ spinner → ✓「こうしん しました」1.5s → 復帰
  - ラベル: 「おさいふを 更新(こうしん)」＋ヒント「（おうちのひとの 承認(しょうにん)を とりこむ）」
  - state 追加: `refreshDoneAt: number | null`
  - 既存の `onRefresh` を spinner 表示＋完了フィードバック対応に拡張
  - **衝突可能性**: 中（同ファイル MBA 側で大改修中の可能性）

